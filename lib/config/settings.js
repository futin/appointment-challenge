// core modules

// 3rd party modules

// internal alias modules

const mongodbSetup = {
  url: 'mongodb://localhost/timify-challenge',
};

const serverSetup = {
  host: 'localhost',
  port: '3000',
};

module.exports = {
  mongodbSetup,
  serverSetup,
};
