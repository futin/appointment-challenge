// core modules

// 3rd party modules

// internal alias modules

// List of namespaces used with debug module
const loggerNamespaces = {
  // App level namespace
  appNamespace: 'timify-challenge:app',
  mongodbSetupNamespace: 'timify-challenge:database:setup',
};

module.exports = {
  loggerNamespaces,
};
